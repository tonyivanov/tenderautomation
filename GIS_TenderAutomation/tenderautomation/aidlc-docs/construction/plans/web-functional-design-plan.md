# Functional Design Plan — Unit 3: Web Application

## Статус выполнения

- [x] Шаг 1: Ответы получены и проанализированы
- [x] Шаг 2: domain-entities.md — пользователи, сессии, веб-сущности
- [x] Шаг 3: business-logic-model.md — алгоритмы аутентификации, экспорта, истории
- [x] Шаг 4: business-rules.md — правила валидации, доступа, UI-поведения
- [x] Шаг 5: frontend-components.md — компоненты Jinja2 UI
- [x] Шаг 6: Валидация PBT-01

---

## Контекст Unit 3

**Stories**: US-03● (скачивание JSONL+AGENTS.md), US-04● (просмотр тендеров, индикатор новых),
US-05● (загрузка AI-анализа в карточку), US-06● (принятие решений + лог), US-07● (история)
**Зависит от**: Unit 1 (`TenderRepository`, `ActionLogRepository`, `QualifiedLogRepository`)
**Stack**: FastAPI + Jinja2 SSR + session-based auth (cookie)

---

## Уточняющие вопросы

Заполните [Answer]: для каждого вопроса.

---

### Q1: Управление пользователями
Как создаются учётные записи специалистов?

A) Конфиг-файл (`.env` или `users.yaml`) — список логинов/паролей, без регистрации через UI
B) Простая admin CLI-команда: `python -m web.cli add-user --username X --password Y`
C) Полноценная admin-страница в веб-интерфейсе — создание/удаление пользователей
X) Другое (опишите после [Answer]:)

[Answer]: B

---

### Q2: Хранение паролей пользователей
Где хранятся хэши паролей специалистов?

A) В PostgreSQL — таблица `users` (id, username, password_hash, role, created_at)
B) В `users.yaml` файле рядом с конфигом — простота, без миграций
C) В PostgreSQL, но только через Alembic-миграцию (отдельная от тендеров схема)
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q3: Формат экспорта для AI-агента (US-03)
Как специалист скачивает файлы для AI-анализа?

A) Один ZIP-архив: `tenders.jsonl` + `AGENTS.md` — одна кнопка «Скачать»
B) Два отдельных эндпоинта: `/export/jsonl` и `/export/agents-md`
C) ZIP по умолчанию, но оба файла доступны и отдельно
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q4: Индикатор «новый» тендер (US-04)
Как система знает, что специалист просмотрел тендер?

A) Таблица `tender_views` в PostgreSQL: (tender_id, user_id, viewed_at) — per-user tracking
B) Глобальный флаг на тендере: `is_new` bool, снимается при первом просмотре любым пользователем
C) Только по дате: тендеры собранные за последние N часов считаются «новыми» (без tracking)
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q5: Пагинация списка тендеров
Как организован список квалифицированных тендеров?

A) Простая пагинация: страницы по 20 тендеров, кнопки «Назад/Вперёд»
B) Бесконечная прокрутка (AJAX/HTMX load more)
C) Фиксированный список последних N тендеров (например 100) без пагинации
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q6: AGENTS.md — статический или генерируемый?
Как формируется файл AGENTS.md для скачивания?

A) Статический файл `AGENTS.md` в корне репозитория — редактируется вручную при необходимости
B) Генерируется динамически: подставляет актуальные semantic rules из `filters/config.yaml`
C) Базовый статический файл + динамически вставляемый блок semantic_profile из config.yaml
X) Другое (опишите после [Answer]:)

[Answer]: C
