# Functional Design Plan — Unit 2: Platform Adapters

## Статус выполнения

- [x] Шаг 1: Ответы получены и проанализированы
- [x] Шаг 2: domain-entities.md — схемы данных платформ, маппинг полей
- [x] Шаг 3: business-logic-model.md — алгоритмы сбора, аутентификации, пагинации
- [x] Шаг 4: business-rules.md — правила, ограничения, анти-бот защита
- [x] Шаг 5: Валидация PBT-01 (тестируемые свойства адаптеров)

---

## Контекст Unit 2

**Stories**: US-01● (полный сбор с обеих площадок)
**Зависит от**: Unit 1 (`PlatformAdapter` ABC, `TenderModel`, `RawTender`, `AuthSession`, `PlatformCredentials`)
**Существующий код**:
- `Разбор тендеров/Bidzaar_parsing/` — auth.py, fetch_list.py, prefilter.py
- `Разбор тендеров/B2bCenter_parsing/` — fetch_list.py, login.py, prefilter.py

**Ключевой факт из анализа кода**:
- **Bidzaar**: auth через Playwright (headless), JWT в localStorage, REST JSON API
- **B2B-Center**: auth не нужна для листинга, HTML-скрейпинг с поиском по ключевым словам

---

## Уточняющие вопросы

Заполните [Answer]: для каждого вопроса.

---

### Q1: Bidzaar — автоматизация логина
Текущий код: Playwright сохраняет состояние сессии вручную. Новое требование (Q7:B): автоматический логин из `.env`.
Как автоматизировать первоначальный логин в Bidzaar?

A) Playwright headless: программно заполняет форму логина (username + password из `.env`), без ручного вмешательства
B) Прямой HTTP-запрос к IdentityServer (ROPC grant): `POST /auth/connect/token` с `grant_type=password` — если площадка поддерживает
C) Оставить Playwright с ручным первым логином, но автоматическое обновление токена — это тоже соответствует требованию «из .env»
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q2: B2B-Center — режим сбора
Текущий код поддерживает два режима: (1) сплошной обход секций, (2) поиск по ключевым словам. В новой системе квалификация происходит после сбора, поэтому нужно решить что собирать.

A) Только поиск по ключевым словам (search_queries.yaml) — забираем только потенциально релевантные, меньше шума и нагрузки
B) Сплошной обход секций (commercial + fz223) — все тендеры, квалификация потом отфильтрует
C) Гибридно: сплошной обход для коммерческих + поиск по ключевым словам для 223-ФЗ
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q3: B2B-Center — нужна ли аутентификация в Unit 2?
Текущий код: auth нужна только для recon_card (полная карточка тендера). Для листинга и поиска — не нужна.
В Unit 2 (Tier 1 qualification) нужны только базовые поля: название, заказчик, НМЦК, дедлайн, URL.

A) Нет — в Unit 2 не реализовывать аутентификацию B2B-Center. Recon карточки — будущая функция (Unit 3 или позже)
B) Да — реализовать аутентификацию сейчас, чтобы можно было расширять
C) Частично — подготовить заглушку auth в дескрипторе, но не реализовывать recon в Unit 2
X) Другое (опишите после [Answer]:)

[Answer]: C

---

### Q4: Bidzaar API — параметры пагинации
Из существующего кода: `GET /api/process/light/procedures/available`. Как работает пагинация?

A) Параметры `page` и `size` (page=0, 1, 2... size=20) — стандартная page-based
B) Параметр `offset` и `limit` — cursor-style
C) Уточнить из существующего кода/документации — я не знаю точно
X) Другое (опишите после [Answer]:)

[Answer]: C

---

### Q5: Защита от шумных выдач B2B-Center
Существующий код: если поисковый запрос вернул >1000 лотов — пропускаем (признак что запрос слишком широкий, выдача не релевантна). Оставляем это правило?

A) Да — оставить ограничение: >1000 результатов → пропустить этот поисковый запрос, залогировать предупреждение
B) Нет — убрать ограничение, собирать всё
C) Настраиваемый порог в дескрипторе платформы (по умолчанию 1000)
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q6: Формат YAML-дескриптора платформы
Что должен содержать файл дескриптора платформы (например `b2bcenter/descriptor.yaml`)?

A) Минимальный: platform_id, base_url, auth_method, rate_limit_sec, field_mappings
B) Расширенный: минимальный + search_queries (список запросов для B2B-Center), api_endpoints (для Bidzaar), платформо-специфичные параметры
C) Разделить на два файла: descriptor.yaml (технические параметры) + queries.yaml (поисковые запросы) — как в существующем коде
X) Другое (опишите после [Answer]:)

[Answer]: C
