# Application Design Plan — TenderAutomation

## Статус выполнения

- [x] Шаг 1: Ответы пользователя получены и проанализированы
- [x] Шаг 2: components.md — компоненты и ответственности
- [x] Шаг 3: component-methods.md — сигнатуры методов
- [x] Шаг 4: services.md — сервисный слой и оркестрация
- [x] Шаг 5: component-dependency.md — зависимости и потоки данных
- [x] Шаг 6: application-design.md — консолидированный документ
- [x] Шаг 7: Валидация полноты и консистентности

---

## Контекст для проектирования

**Система**: 4 unit'а — Core Library, Platform Adapters, Web Application, Notification Service  
**Tech base**: Python, SQLite (существующий), JSONL-логи, Linux VPS  
**Ограничения**: Security Baseline (блокирующий), PBT (блокирующий)

---

## Уточняющие вопросы

Заполните [Answer]: для каждого вопроса.

---

### Q1: Веб-фреймворк
Какой Python-фреймворк использовать для веб-приложения и REST API?

A) FastAPI — современный, async, автодокументация OpenAPI, хорошо для API + Jinja2 для UI
B) Flask — легковесный, проверенный, большая экосистема, Jinja2 для UI
C) Django — batteries-included, встроенная admin-панель, ORM, сложнее для API
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q2: Фронтенд-подход
Как реализовать веб-интерфейс для специалистов?

A) Server-side rendering (Jinja2-шаблоны) + минимальный JavaScript — простота, SEO не нужен
B) HTMX + Jinja2 — интерактивность без полного SPA, минимальный JS, современный подход
C) Лёгкий SPA (Vue.js или Alpine.js) + REST API — больше интерактивности, отдельный фронтенд
D) React/Next.js + REST API — полноценный SPA (сложнее, но максимальная гибкость)
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q3: Аутентификация пользователей
Как организовать вход специалистов в систему?

A) Session-based auth: логин/пароль → серверная сессия (cookie) — просто, надёжно, без токенов
B) JWT-токены: логин → JWT в localStorage/cookie — stateless, удобно для API-клиентов
C) HTTP Basic Auth + HTTPS — минимальная сложность для небольшой команды
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q4: Планировщик сбора тендеров
Как запускать автоматический сбор по расписанию?

A) Системный cron (Linux cron job) вызывает Python-скрипт — просто, независимо от веб-приложения
B) APScheduler внутри веб-процесса — планировщик встроен в приложение, единый процесс
C) Celery + Redis/RabbitMQ — очередь задач, надёжность, но сложнее в эксплуатации
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q5: Структура проекта
Как организовать код в репозитории?

A) Монорепо с пакетами: `src/core/`, `src/adapters/`, `src/web/`, `src/notifications/` — один репо, единая зависимость
B) Монорепо с namespace packages: каждый unit — отдельный устанавливаемый пакет (`pip install -e .`)
C) Плоская структура: модули на верхнем уровне без вложенных пакетов — проще для небольшого проекта
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q6: Хранилище данных
Подтвердите подход к хранению (исходя из требований):

A) SQLite для тендеров и пользователей + JSONL-файлы для append-only логов (actions, qualified tenders)
B) Только SQLite — все данные в БД, включая логи (таблицы вместо JSONL-файлов)
C) PostgreSQL для основных данных + JSONL для логов — если планируется горизонтальное масштабирование
X) Другое (опишите после [Answer]:)

[Answer]: C

---

### Q7: Platform Adapter — интерфейс
Как должен выглядеть контракт между Core и Platform Adapters?

A) Абстрактный базовый класс (ABC) `PlatformAdapter` с методами `fetch_new()`, `authenticate()`, `get_tender_detail()`
B) Protocol (structural subtyping, Python typing) — duck typing без наследования
C) Простые функции в отдельном модуле для каждой площадки — без формального интерфейса
X) Другое (опишите после [Answer]:)

[Answer]: A

---

### Q8: Notification Service — интеграция
Как Notification Service узнаёт о новых квалифицированных тендерах?

A) Polling: периодически читает БД/JSONL и отправляет уведомления по изменениям
B) Events: Core публикует событие (in-process callback/signal) после квалификации — Notification подписывается
C) Cron: отдельный cron job запускает notification скрипт по расписанию (раз в N минут)
X) Другое (опишите после [Answer]:)

[Answer]: B

---

# Текущая итерация — Export Integrity

## План Application Design

- [x] Проанализировать утверждённые требования и execution plan.
- [x] Проследить текущие зависимости ExportService, PlatformAdapter,
  TenderRepository, V3 cache и reconciliation через исходный код и Graphify.
- [x] Получить и проверить ответы на архитектурные вопросы ниже.
- [x] Обновить `components.md`: новые компоненты и ответственности.
- [x] Обновить `component-methods.md`: высокоуровневые сигнатуры и типы.
- [x] Обновить `services.md`: async orchestration и error/result flow.
- [x] Обновить `component-dependency.md`: зависимости и communication patterns.
- [x] Обновить консолидированный `application-design.md`.
- [x] Проверить согласованность с Security Baseline, PBT и requirements.
- [x] Валидировать Markdown и все диаграммы.

## Архитектурные вопросы

### Q9: Представление архивного состояния
Как хранить состояние процедуры, не смешивая его с пользовательским workflow
(`taken`, `deferred`, `rejected`)?

A) Добавить отдельное поле `procedure_state` и вычислять Архив из него и дедлайна;
workflow-статус оставить неизменным (рекомендуется)

B) Добавить `archived` непосредственно в существующий `TenderStatus`

C) Не хранить состояние; вычислять его только во время каждого запроса

X) Другое (опишите после тега [Answer]: ниже)

[Answer]: A

### Q10: Источник инструкции для AI ZIP
Где должен находиться runtime-шаблон `AGENTS.md`, включаемый в экспорт?

A) В отдельном versioned-файле `prompts/tender_analysis_agents.md`, который
явно копируется в image; корневой `AGENTS.md` остаётся инструкцией разработчикам
и агентам репозитория (рекомендуется)

B) Продолжать использовать корневой `AGENTS.md`, добавив его в Docker image

C) Хранить шаблон в переменной окружения

X) Другое (опишите после тега [Answer]: ниже)

[Answer]: A

### Q11: Источник контекста V3
Откуда export flow должен получать финальную классификацию V3?

A) Через отдельный read-only projection component поверх существующего SQLite
V3 cache, с единым правилом выбора judge и queue (рекомендуется)

B) Перенести V3-классификацию в PostgreSQL в этой итерации

C) Передавать V3-поля из web router в ExportService

X) Другое (опишите после тега [Answer]: ниже)

[Answer]: A

### Q12: Результат заблокированного экспорта
Как browser flow должен сообщать, что ZIP не сформирован?

A) Возвращать безопасную HTML-страницу результата: 409 для неактивного тендера
и 503 для неподтверждённого из-за внешнего сбоя, со ссылкой назад (рекомендуется)

B) Перенаправлять на карточку тендера с flash-сообщением

C) Возвращать JSON error response

X) Другое (опишите после тега [Answer]: ниже)

[Answer]: A
