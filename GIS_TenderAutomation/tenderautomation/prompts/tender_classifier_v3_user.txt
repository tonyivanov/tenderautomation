Проанализируй следующие заголовки тендеров и классифицируй каждый по правилам из system prompt.

Возврати ТОЛЬКО JSON-массив объектов, без текста до или после.

Каждый объект должен содержать:
- id: ID тендера из входных данных
- verdict: "core" / "review" / "reject"
- fit_score: 0-100 (степень соответствия компетенциям Git in Sky)
- confidence: 0.0-1.0 (уверенность в verdict)
- procurement_type: "services" / "mixed" / "supply_only" / "licenses_only" / "unknown"
- primary_domain: один из доменов
- secondary_domains: массив строк (дополнительные домены)
- needs_documents: true/false (нужно ли открыть ТЗ)
- positive_evidence: массив строк (до 3 ключевых слов/фраз из заголовка, указывающих на релевантность)
- negative_evidence: массив строк (до 3 указаний на нерелевантность)
- risk_flags: массив строк (флаги риска)
- reason: краткое объяснение (1-2 предложения на русском)

Тендеры:
{{TENDERS_JSON}}